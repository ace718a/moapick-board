# MOAPICK 콘텐츠 발행 작업 지침

이 문서는 `moapick` 사이트에 신규 페이지를 작성·수정·배포할 때 반드시
따라야 하는 작업 규칙이다. 기존 HTML/CSS 디자인 구조는 재사용할 수
있지만, 콘텐츠 품질·지역성·이미지·링크·배포 파일은 아래 기준으로 반드시
검수한다.

## 1. 최우선 원칙

1.  **기존 사이트의 디자인과 구조를 임의로 변경하지 않는다.**
2.  사용자가 요청한 파일만 수정한다. 불필요한 파일, 딜레이, 스크립트,
    설정값 등을 임의로 건드리지 않는다.
3.  신규 페이지는 기존 페이지의 **HTML/CSS 골격은 재사용 가능**하지만,
    본문을 단순 치환해서 만들지 않는다.
4.  지역 롱테일 페이지는 **지역명만 바꾼 복제 페이지를 절대 만들지
    않는다.**
5.  작업 완료를 선언하기 전에 실제 배포 ZIP 기준으로 최종 검수한다.
6.  검수하지 않은 항목을 `PASS`, `정상`, `문제없음`이라고 표현하지
    않는다.

------------------------------------------------------------------------

## 2. 지역 롱테일 콘텐츠 작성 규칙

예: - 부산 강서구 포장이사 - 부산 동래구 포장이사 - 해운대구 코웨이
정수기렌탈 - 기장군 인터넷가입 사은품 - 사하구 하단동 인터넷가입 사은품

### 반드시 해야 할 것

지역 페이지를 작성하기 전에 해당 지역의 실제 특성을 조사한다.

조사 대상은 주제와 직접 관계있는 정보만 사용한다.

예를 들어 포장이사라면: - 아파트·공동주택 밀집 지역 - 기존 주택/저층
주거지역 - 주요 생활권 - 차량 진입 및 이동 동선과 관련 있는 지역 구조 -
신도시/구도심 등 주거 형태 차이

인터넷가입이라면: - 아파트·오피스텔·원룸·다가구 등 주거 형태 -
대학가/역세권/신도시 등 실제 생활권 - 공동주택과 개별주택의 설치환경
차이 - 실제 주소 조회가 중요한 이유

정수기렌탈이라면: - 공동주택/기존 주택 등 설치환경 - 주방 구조와 설치
공간에 영향을 줄 수 있는 주거 형태 - 지역 내 대표적인 주거 생활권

### 지역정보 사용 원칙

지역정보를 단순 장식처럼 나열하지 않는다.

나쁜 예: \> 하단동에는 동아대학교가 있습니다. 인터넷가입 사은품을
비교하세요.

좋은 예: \> 동아대학교 주변 원룸·다가구 주택은 건물에서 인터넷을 일괄
제공하는 경우가 있을 수 있으므로 신규가입 전에 관리비 포함 여부와 개별
회선 설치 가능 여부를 먼저 확인합니다.

즉, **지역 특성 → 해당 서비스 이용 시 실제 확인사항**으로 연결한다.

------------------------------------------------------------------------

## 3. 본문 재사용 금지

지역 페이지만 바꿔가며 다음과 같이 작업하지 않는다.

-   강서구 → 동래구 지역명만 치환
-   기장군 → 하단동 지역명만 치환
-   기존 소제목 순서 그대로 유지하며 지명만 변경
-   문단 대부분을 복사하고 몇 문장만 변경

HTML 레이아웃, CSS 클래스, CTA 구조는 재사용할 수 있다.

그러나 다음은 신규 작성한다.

-   제목
-   메타 description
-   OG description
-   도입부
-   요약
-   각 소제목
-   각 본문 문단
-   지역 특화 설명
-   마지막 체크리스트/정리 문구

### 중복 검사

신규 원고 작성 후 같은 카테고리의 기존 페이지들과 본문 유사도를
검사한다.

특정 기존 지역 페이지와 지나치게 높은 유사도가 나오면 다시 작성한다.

숫자 하나만으로 품질을 판단하지 말고 실제 공통 문단도 함께 확인한다.

------------------------------------------------------------------------

## 4. 지역명 오염 검사

신규 지역 페이지 작성 후 반드시 다른 지역명이 잘못 남아 있는지 검사한다.

예: - 동래구 페이지에 `명지`, `녹산`, `신호`가 이유 없이 남아 있음 →
실패 - 하단동 페이지에 `기장읍`, `정관읍`, `일광읍`이 남아 있음 → 실패

단, 비교나 이동 설명 등 문맥상 실제로 필요한 타 지역명은 예외다.

지역명 오염이 발견되면 배포하지 않는다.

------------------------------------------------------------------------

## 5. SEO 기본 규칙

신규 페이지마다 다음을 확인한다.

-   `<title>`
-   `<h1>`
-   meta description
-   OG title/description
-   canonical
-   구조화 데이터가 존재한다면 headline/url/image
-   본문 내 핵심 키워드의 자연스러운 사용
-   과도한 키워드 반복 금지

지역 키워드는 제목에만 넣지 말고 본문에서도 자연스럽게 사용한다.

단, SEO를 이유로 같은 키워드를 억지로 반복하지 않는다.

------------------------------------------------------------------------

## 6. 썸네일 제작 절대 규칙

### 원본 이미지

사용자가 제공한 **글자 없는 원본 이미지**가 있다면 그것을 최우선으로
사용한다.

절대 하지 말 것:

-   이미 텍스트가 박혀 있는 기존 썸네일을 배경으로 재사용
-   기존 썸네일을 블러 처리해 새 썸네일 배경으로 사용
-   기존 글자가 희미하게 보이는 상태에서 새 글자를 덮기
-   사용자가 준 원본을 무시하고 임의의 다른 이미지를 사용

### 기본 형식

별도 요청이 없으면:

-   **정사각형**
-   대표 텍스트는 **정중앙**
-   작은 화면에서도 보이도록 **크고 굵게**
-   배경 이미지는 배경 역할에 집중
-   잡다한 보조문구 최소화
-   핵심 키워드 중심

### 이미지 생성 후 필수 육안 검수

파일이 존재한다고 이미지 검수가 끝난 것이 아니다.

최종 이미지를 **실제로 열어서 눈으로 확인한다.**

확인 항목:

-   정사각형인가
-   문구가 정확한가
-   한글이 깨지지 않았는가
-   기존 텍스트가 배경에 남아 있지 않은가
-   중앙 정렬이 자연스러운가
-   글자가 잘리지 않았는가
-   모바일 썸네일 크기에서도 읽을 수 있는가
-   불필요한 문구가 들어가지 않았는가
-   요청한 배경 이미지가 맞는가

하나라도 문제가 있으면 다시 만든다.

------------------------------------------------------------------------

## 6-1. 본문 보조 이미지 레이아웃 규칙

상세페이지 본문 중간에 들어가는 보조 이미지는 상단 대표이미지와 용도가 다르므로 **대표이미지용 `article-cover` 클래스를 재사용하지 않는다.**

- 상단 대표이미지: `article-cover`
- 본문 중간 보조이미지: `article-inline-image`
- 본문 이미지 앞뒤에는 다음 소제목과 붙어 보이지 않도록 충분한 세로 여백을 둔다.
- 데스크톱뿐 아니라 모바일에서도 이미지가 본문 폭을 벗어나거나 화면 끝까지 강제로 확장되지 않는지 확인한다.
- 본문 이미지 직후 `step-number + h2`가 시작될 경우 이미지 하단과 소제목 사이 간격을 반드시 육안으로 확인한다.
- 새 클래스를 적용할 때 기존 대표이미지의 CSS를 변경해 다른 상세페이지까지 영향을 주지 않는다.
- 이미지 삽입 후 **이미지 위 문단 → 이미지 → 다음 소제목 → 다음 문단** 흐름을 실제 브라우저 화면 기준으로 확인한다.
- HTML에 이미지 파일이 존재하고 경로가 맞는 것만으로 레이아웃 검수를 PASS 처리하지 않는다.

### 본문 보조 이미지 최종 검수

1. 이전 문단과 이미지 사이가 지나치게 넓거나 좁지 않은가
2. 이미지와 다음 소제목이 붙어 있지 않은가
3. 이미지 폭과 좌우 정렬이 기존 상세페이지 디자인과 어울리는가
4. 모바일에서 불필요한 음수 마진이나 화면 넘침이 없는가
5. 대표이미지용 클래스가 본문 이미지에 잘못 적용되지 않았는가

위 항목 중 하나라도 확인하지 않았다면 이미지 레이아웃을 `PASS`라고 하지 않는다.

---

## 7. 이미지 파일명 및 HTML 경로

이미지를 만든 뒤 반드시 실제 HTML과 경로를 대조한다.

예:

`assets/images/saha-hadan-internet-signup-benefits.png`

HTML: `/assets/images/saha-hadan-internet-signup-benefits.png`

두 경로가 정확히 일치해야 한다.

### 이미지 교체 시 캐시

기존과 동일한 파일명으로 이미지를 교체하면 브라우저/CDN 캐시 때문에 이전
이미지가 보일 수 있다.

필요하면 다음처럼 버전 쿼리를 변경한다.

`image.png?v=2` → `image.png?v=3`

단, HTML 참조와 실제 파일명을 혼동하지 않는다. 실제 파일은 `.png`,
쿼리는 HTML URL에만 붙는다.

------------------------------------------------------------------------

## 8. 카테고리별 CTA 검수

CTA는 페이지 주제에 맞는 경로를 사용한다.

현재 기본 내부 경로:

-   포장이사 → `/24/`
-   장기렌트 → `/rent`
-   정수기렌탈 → `/water/`
-   인터넷가입 → `/internet`

외부 상담 iframe/제휴 링크는 기존 사이트에서 사용 중인 정상 코드를
기준으로 유지한다.

### 금지

-   `href=""`
-   링크 없는 CTA 버튼
-   다른 카테고리 CTA 연결
-   기존 정상 링크를 이유 없이 변경
-   사용자가 지정한 CTA를 임의로 다른 주소로 변경

------------------------------------------------------------------------

## 9. 신규 페이지 발행 시 연결 파일

신규 페이지를 추가할 때 현재 사이트 구조에 맞춰 필요한 연결 파일을
확인한다.

일반적으로 확인할 항목:

1.  신규 상세페이지 HTML
2.  신규 썸네일 이미지
3.  해당 카테고리 목록 페이지
4.  메인 최신글 목록
5.  sitemap.xml

기존 사이트 구조가 변경된 경우 현재 최신 ZIP의 구조를 먼저 확인하고 그
구조를 따른다.

과거 기억만 믿고 경로를 추정하지 않는다.

------------------------------------------------------------------------

## 10. 최종 배포 ZIP 검수

작업 폴더가 정상이라고 바로 전달하지 않는다.

**사용자에게 실제 전달할 ZIP 파일을 만든 뒤 다시 압축을 풀어 최종
결과물을 검사한다.**

### HTML 검수

-   파일 경로 정상
-   HTML 문법상 치명적 누락 없음
-   title/H1 정상
-   지역명 정상
-   다른 지역명 오염 없음
-   이미지 경로 정상
-   CTA 정상
-   빈 링크 없음
-   카테고리 목록 연결 정상
-   메인 연결 정상
-   sitemap 연결 정상

### 콘텐츠 검수

-   지역명만 치환한 글이 아닌가
-   실제 지역 특성이 들어갔는가
-   지역 특성이 서비스 주제와 연결되는가
-   기존 글과 문단이 과도하게 겹치지 않는가
-   다른 페이지 문장이 실수로 남아 있지 않은가

### 이미지 검수

최종 ZIP 안의 이미지를 직접 열어서 확인한다.

-   이미지 자체가 정상인가
-   올바른 썸네일인가
-   이전 페이지 글자가 남아 있지 않은가
-   문구/정렬/가독성이 정상인가
-   HTML이 그 파일을 실제로 참조하는가

------------------------------------------------------------------------

## 11. PASS 판정 기준

다음처럼 하지 않는다.

> QA PASS 문제없음 검수 완료

위 표현은 **실제로 해당 검사를 수행한 경우에만** 사용한다.

특히 이미지가 포함된 작업에서 이미지를 직접 열어보지 않았다면 이미지
검수 PASS라고 하지 않는다.

자동 스크립트 PASS와 최종 품질 검수는 별개다.

가능하면 결과를 다음처럼 구분한다.

-   구조 검사: PASS
-   링크 검사: PASS
-   콘텐츠 중복/지역 오염 검사: PASS
-   이미지 파일 검사: PASS
-   이미지 육안 검사: PASS
-   최종 ZIP 재검수: PASS

모든 필수 항목이 끝난 뒤에만 최종 완료로 판단한다.

------------------------------------------------------------------------

## 12. 사용자가 최신 ZIP을 제공했을 때

최신 ZIP이 제공되면 그것을 **유일한 기준본**으로 사용한다.

1.  ZIP 구조 확인
2.  현재 페이지/이미지/CTA/목록 구조 확인
3.  필요한 파일만 수정
4.  과거 작업본을 최신 ZIP 위에 무작정 덮어 합치지 않는다.
5.  최신 ZIP에 없는 과거 파일을 기억에 의존해 복원하지 않는다.

사용자가 "업로드해야 하는 파일만 달라"고 하면 변경된 파일만 ZIP에
넣는다.

------------------------------------------------------------------------

## 13. 작업 방식 요약

신규 지역 페이지의 기본 순서는 다음과 같다.

**최신본 확인 → 지역 조사 → 검색 의도 결정 → 신규 원고 작성 → 기존
디자인에 적용 → 지역명 오염 검사 → 중복 검사 → 썸네일 제작 → 썸네일 육안
검수 → 이미지 경로 확인 → CTA 확인 → 목록/메인/sitemap 연결 → 배포 ZIP
생성 → ZIP 재압축 해제 → 최종 HTML·이미지 육안 검수 → 전달**

이 순서를 생략해서 빠르게 만드는 것보다, 한 페이지씩 정확하게 만드는
것을 우선한다.
